import React from 'react';import{createContext,useContext,useEffect,useState}from'react';import{api}from'../api/client';const C=createContext();export function AuthProvider({children}){const[user,setUser]=useState(null);const[loading,setLoading]=useState(true);useEffect(()=>{api.get('/auth/me').then(r=>setUser(r.data.user)).catch(()=>{}).finally(()=>setLoading(false))},[]);const login=async(credentials)=>{const r=await api.post('/auth/login',credentials);setUser(r.data.user)};const logout=async()=>{await api.post('/auth/logout');setUser(null)};return <C.Provider value={{user,login,logout,loading}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);
