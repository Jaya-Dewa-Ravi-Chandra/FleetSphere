import React from 'react';import CrudPage from '../components/CrudPage';
export const Branches=()=> <CrudPage title="Branches" endpoint="/branches" columns={[{key:'name',label:'Branch'},{key:'code',label:'Code'},{key:'city',label:'City'},{key:'state',label:'State'},{key:'status',label:'Status'}]} createFields={[{key:'name',label:'Name',required:true},{key:'code',label:'Code',required:true},{key:'address',label:'Address'},{key:'city',label:'City'},{key:'state',label:'State'},{key:'contact',label:'Contact'}]}/>;
export const Users=()=> <CrudPage title="Users" endpoint="/users" columns={[{key:'name',label:'Name'},{key:'email',label:'Email'},{key:'role',label:'Role'},{key:'active',label:'Active'}]} createFields={[{key:'name',label:'Name',required:true},{key:'email',label:'Email',type:'email',required:true},{key:'password',label:'Temporary password',type:'password',required:true},{key:'role',label:'Role',type:'select',options:[{value:'fleet_manager',label:'Fleet Manager'},{value:'branch_manager',label:'Branch Manager'},{value:'driver',label:'Driver'},{value:'finance_officer',label:'Finance Officer'},{value:'super_admin',label:'Super Admin'}]}]}/>;
export function Settings(){return <div className="card settings"><h1>Settings</h1><p>FleetSphere environment and account controls are managed through secure server configuration.</p><div className="notice">Use <code>server/.env</code> for secrets and <code>client/.env</code> for the API URL. Never commit production secrets.</div></div>}
export function AuditLogs(){return <CrudPage title="Audit Logs" endpoint="/audit-logs" columns={[{key:'action',label:'Action'},{key:'entityType',label:'Entity'},{key:'userId.name',label:'User'},{key:'timestamp',label:'Time',render:x=>x.timestamp?new Date(x.timestamp).toLocaleString():'—'}]} createFields={[]}/>}
