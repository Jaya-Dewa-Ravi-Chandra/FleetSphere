import React from 'react';import{useState}from'react';import{useAuth}from'../context/AuthContext';import{useNavigate}from'react-router-dom';import{Truck,Shield,Wallet,UserRound,Building2}from'lucide-react';const roles=[['Super Admin','superadmin@fleetsphere.com',Shield],['Fleet Manager','fleetmanager@fleetsphere.com',Truck],['Branch Manager','branchmanager@fleetsphere.com',Building2],['Driver','driver@fleetsphere.com',UserRound],['Finance Officer','finance@fleetsphere.com',Wallet]];export default function Login(){const{login}=useAuth();const nav=useNavigate();const[email,setEmail]=useState('superadmin@fleetsphere.com');const[pw,setPw]=useState('Fleet@123');const[err,setErr]=useState('');const submit=async e=>{e.preventDefault();try{await login({email,password:pw});nav('/dashboard')}catch(x){setErr(x.message||'Login failed')}};return <div className="auth"><div className="login-card"><div className="logo">F</div><h1>Welcome to FleetSphere</h1><p>One Fleet. Complete Control.</p><form onSubmit={submit}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required/></label><label>Password<input value={pw} onChange={e=>setPw(e.target.value)} type="password" required/></label>{err&&<div className="error">{err}</div>}<button className="primary">Sign in</button></form><div className="demo"><b>Demo accounts · password Fleet@123</b>{roles.map(([n,e,I])=><button key={e} onClick={()=>{setEmail(e);setPw('Fleet@123')}}><I size={16}/><span>{n}<small>{e}</small></span></button>)}</div><button type="button" className="secondary" onClick={()=>nav('/auth/register')}>Create account</button></div></div>}
