import React from 'react';import{useEffect,useState}from'react';import{api}from'../api/client';import StatCard from'../components/StatCard';import FleetMap from'../components/FleetMap';import{ResponsiveContainer,AreaChart,Area,XAxis,YAxis,Tooltip}from'recharts';
export default function Dashboard(){const[d,setD]=useState(null),[err,setErr]=useState('');useEffect(()=>{api.get('/analytics/dashboard').then(r=>setD(r.data)).catch(e=>setErr(e.message))},[]);return <><div className="pagehead"><div><h1>Operations overview</h1><p>Live fleet activity and operational performance.</p></div><span className="live">● LIVE</span></div>{err&&<div className="error page-error">{err}</div>}<div className="stats"><StatCard label="Total vehicles" value={d?.kpis?.vehicles??'—'} sub="Fleet inventory"/><StatCard label="Active vehicles" value={d?.kpis?.activeVehicles??'—'} sub="Currently on trip"/><StatCard label="Maintenance" value={d?.kpis?.maintenanceVehicles??'—'} sub="Requires attention"/><StatCard label="Available" value={d?.kpis?.availableVehicles??'—'} sub="Ready for allocation"/><StatCard label="Fuel cost" value={d?`₹${Number(d.kpis.fuelCost).toLocaleString()}`:'—'} sub="Recorded fuel spend"/><StatCard label="Maintenance cost" value={d?`₹${Number(d.kpis.maintenanceCost).toLocaleString()}`:'—'} sub="Recorded service spend"/></div><div className="grid2"><div className="card"><div className="cardhead"><h2>Fleet activity</h2><span>Last 7 days · live data</span></div><ResponsiveContainer width="100%" height={280}><AreaChart data={d?.trend||[]}><XAxis dataKey="date"/><YAxis/><Tooltip/><Area type="monotone" dataKey="trips" fill="currentColor" fillOpacity=".12" stroke="currentColor"/></AreaChart></ResponsiveContainer></div><div className="card map"><div className="cardhead"><h2>Fleet map</h2><span>OpenStreetMap · live vehicle data</span></div><FleetMap/></div></div></>}
